[package]
name = "indoc-tests"
version = "0.0.0"
authors = ["David Tolnay <dtolnay@gmail.com>"]
edition = "2018"
publish = false

[features]
unstable = ["indoc/unstable"]

[dependencies]
indoc = { path = ".." }

[dev-dependencies]
indoc = { path = ".." }
select-rustc = "0.1"
trybuild = "1.0"
